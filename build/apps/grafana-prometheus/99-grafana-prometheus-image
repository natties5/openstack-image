#!/bin/sh
IP=$(hostname -I 2>/dev/null | awk '{print $1}')
cat << EOF

ระบบเฝ้าระวัง Grafana + Prometheus
===================================
Grafana:   http://${IP:-<VM-IP>}/
รหัสผ่าน:  sudo monitoring-info
ดูสถานะ:   sudo monitoring-status

อ่านไฟล์: /root/README-grafana-prometheus-image.txt
EOF
